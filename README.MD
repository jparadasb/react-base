REACT BASE TO PROJECTS
======================

This is a personal base to projects with react.

If you want to use in development you can run this script with npm:

```
npm run start
```
By default runs in localhost:8000

For build:

```
npm run build
```
